CREATE TABLE routes_rated (
	name_id integer,
	country varchar,
	crag varchar,
	sector varchar,
	name varchar,
	tall_recommend_sum integer,
	grade_mean decimal,
	cluster integer,
	rating_tot decimal
);